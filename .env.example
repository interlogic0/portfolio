# InterLogic Portfolio — no environment variables required.
#
# This site is fully static at build time. Copy this file only if your
# tooling expects .env.example to exist; you do not need a .env.local file.
